$('#posts-container').html('');
$('#posts-container').html('<%=j render @posts%>');

// event 재등록
show_post_item_event();

// 여기엔 재등록 할 필요 없다. 클릭 못함
$('#posts-container').prepend("<%=j render 'projects/projects' %>");

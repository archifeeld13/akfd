<% if @post.errors.any? %>
	var errors = []
	<% @post.errors.full_messages.each  do |e| %>
		errors.push("<%=e %>")	
	<% end %>
	alert(errors.join(','));
<% else %>
		$('#modal_bg').click();
		// 여기 나중에 쇼우로 바꾸기
		location.reload()
<% end %>
	

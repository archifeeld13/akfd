<% if @post.post_type == 0 %>
$('#modal_board').html("")

$('<%=j render 'textup' %>').appendTo($('#modal_board'))

// modal close button click
// modal close button click
$('.modal_close').click(function(){
	$('#modal_bg').click();
})	
// modal close button click end

changeWidthHeightModalBoard();

// 모달창의 높이를 초기상태로 돌려준다
// show modal이 길 때, edit모드로 전환하면 긴 부분이 남아있는 현상 해결
$('#modal_board').css('height', 'auto')
$('#modal_board_wrapper').css('height', 'auto')

$('#modal_board').append('<script src="/javascripts/summernote-ko-KR.js"></script>')

$('#input_post_content').summernote({
	height: 200,
	minHeight: 200,
	maxHeight: 200,
	toolbar:[
		['style', ['bold', 'underline', 'clear']],
		['font', ['strikethrough']],
    ['color', ['color']],
		['para', ['ul', 'ol', 'paragraph']],
    ['fontsize', ['fontsize']],
	],
	lang: 'ko-KR',
	placeholder: '이곳에 글을 작성해 주세요' 
});


// show.js.erb실행 시킨 이후에
// 서머노트 내부에서 스크롤 안되는 경우 
prevetScrollPropFrom('.note-editable')
<% elsif @post.post_type == 1 %>
	alert('죄송합니다! 이미지 타입의 게시글에 대한 수정 기능은 개발중입니다 :\'(')	
<% end %>

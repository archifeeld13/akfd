<!DOCTYPE html>
<html>
<head>
  <title>Archifeeld</title>
	<link rel="icon" type="image/png" href="favicon.png">

	<%= stylesheet_link_tag "https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css" %>
	<%= stylesheet_link_tag 'navbarcolor.css' %>
	<%= stylesheet_link_tag 'searchbox.css' %>

	<!-- 합쳐지고 최소화된 최신 자바스크립트 -->
	<%= javascript_include_tag "https://code.jquery.com/jquery-1.12.0.js" %>
	<%= javascript_include_tag "https://code.jquery.com/ui/1.11.4/jquery-ui.js" %>

	<%= javascript_include_tag "https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js" %>

  <%= csrf_meta_tags %>

	<style>
	form
	{
			max-width: 300px;
			/*padding: 15px; */
			margin: 0 auto;
	}
	form .form-control
	{
			position: relative;
			font-size: 16px;
			height: auto;
			padding: 10px;
			-webkit-box-sizing: border-box;
			-moz-box-sizing: border-box;
			box-sizing: border-box;
	}
	form .form-control:focus
	{
			z-index: 2;
	}
	form input[type="text"]
	{
			margin-bottom: -1px;
	}
	form input[type="password"]
	{
			margin-bottom: 0;
	}

	body {
		background-color: #fff;
		color: white;
		background: url('<%= image_url('loginbg.jpg') %>');
		background-size: 100%;
	}
	#loginP {
		margin-top: 50px;
		width: 55%;
	}
	.bgalpha {
		position: absolute;
		top: 0;
		left: 0;
		width:100%;
		background: rgba(0,0,0,0.4);
	}

	#img_size {
		width: 30%;
	}

	.login_btns{
		width: 300px;
	}

	.reg_cancel{
		color: white;	
	}
	</style>
	<script>
		$(function(){
			$('.bgalpha').css('height', $(window).height());

			$(window).resize(function(){
				$('.bgalpha').css('height', $(window).height());
			})
		})	
	</script>
</head>

<body>
<div class="bgalpha"> 
	<center>
		<div id="loginP" align="center">	
			<img id="img_size" src="<%= image_url('loginlogo.png') %>">
			<div>
			<%= form_for(@user) do |f| %>
				<div class="form-inputs">
					<%= f.text_field :email, placeholder: "이메일 주소" , class: "form-control"%>
					<%= f.text_field :name, placeholder: "이름", class: "form-control" %>
					<%= f.password_field :password, placeholder: "비밀번호", required: true, class: "form-control" %>
					<%= f.password_field :password_confirmation, placeholder: "비밀번호 확인", required: true, class: "form-control" %>
				</div>
				<div class="form-actions">
					<%= f.submit :submit, class: "hidden" %>
				</div>
			<% end %>
			</div>
			<div style="margin-top: 10px">
				<%= link_to image_tag('registered.png', class: "login_btns"), '#' %> 
			</div>
			<div style="margin-top: 20px">
				<%= link_to '취소', url_for(:back), class: "reg_cancel" %>
			</div>
		</div>
	</center>
</div>
</body>

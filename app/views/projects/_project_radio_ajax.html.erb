<div id="project_<%=project.id%>">
	<%= radio_button_tag('post[project_id]', project.id) %>
	&nbsp;<%= truncate(project.name, length: 8, separater: ' ')%>&nbsp;
	<%= link_to "x", [current_user, project], method: :delete, remote: true, data: { confirm:	"Are you sure?" } %>
</div>
